# QLoRA fine-tuning stack for the Qwen3-8B → Kryon offensive agent FT.
# torch is NOT listed here — it comes from the base image (kryon runtime ships
# torch 2.12+cu130, validated on Blackwell sm_120). Only the HF QLoRA layer.
#
# Standard Hugging Face QLoRA path (peft + bitsandbytes + trl), NOT Unsloth:
# Unsloth's custom Triton kernels are unproven on sm_120 / CUDA 13, and the
# plain HF stack is the safe baseline. Unsloth can be evaluated later as a
# speed optimization once this path is green.
bitsandbytes>=0.48
transformers>=4.46
peft>=0.14
trl>=0.12
datasets>=3.0
accelerate>=1.0
