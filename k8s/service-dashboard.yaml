apiVersion: v1
kind: Service
metadata:
  name: kryon-dashboard
  namespace: kryon-system
  labels:
    app.kubernetes.io/name: kryon
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: kryon
    app.kubernetes.io/component: dashboard
